# Configuración de npm para este proyecto
# Tailwind CSS v4 requiere dependencias opcionales (binarios nativos)
# better-sqlite3 es opcional (solo si quieres SQLite como fallback)
# Por defecto, instalamos opcionales para que Tailwind funcione
optional=true
