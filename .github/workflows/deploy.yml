name: Deploy Next.js Landing

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.9
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_KEY }}
          port: 22
          script: |
            set -e

            echo "🚀 Entrando a /var/www/spsolutions.app..."
            cd /var/www/spsolutions.app

            echo "🔄 Actualizando repositorio existente..."
            git fetch --all
            git reset --hard origin/main

            echo "📄 Copiando variables de entorno..."
            if [ -f /etc/spsolutions.app/.env.production ]; then
              cp /etc/spsolutions.app/.env.production .env
            else
              echo "⚠️ .env.production no encontrado. Abortando deploy."
              exit 1
            fi

            echo "🛠 Instalando dependencias..."
            export HOME=/root
            npm install --legacy-peer-deps

            echo "🔨 Construyendo la app..."
            npm run build

            echo "♻️ Reiniciando PM2..."
            pm2 delete nextjs 2>/dev/null || true
            PORT=3000 pm2 start npm --name nextjs -- start
            pm2 save

            echo "✅ Deploy finalizado correctamente"
